extends layout

block content
  h1= title

  ul
    each graphicscard in graphicscard_list
      li
        a(href=graphicscard.url) #{graphicscard.name} (#{graphicscard.brand}) - ₹#{graphicscard.price}

    else
      li There are no graphics cards.
